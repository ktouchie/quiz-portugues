package com.ktouchie.quizportugues

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder to confirm the JUnit test source set is wired up correctly. Real unit tests
 * (SM-2 algorithm, gamification logic, content loaders) land in separate tasks.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
