package com.ktouchie.quizportugues.content

/** How a single question is presented — decided per item, per attempt (docs/MOBILE_APP_SPEC.md §9). */
enum class QuestionModality {
    MULTIPLE_CHOICE,
    TYPED,
}
