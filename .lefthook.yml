pre-commit:
  commands:
    lint:
      run: |
        export PATH="$HOME/.local/share/fnm:$PATH"
        eval "$(fnm env --shell zsh)"
        npm run lint
    test:
      run: |
        export PATH="$HOME/.local/share/fnm:$PATH"
        eval "$(fnm env --shell zsh)"
        npm test
